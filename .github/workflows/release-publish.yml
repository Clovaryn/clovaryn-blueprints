name: Release Publish Reserved

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Reserved for future registry publishing
        run: |
          echo "Publishing is intentionally disabled in Phase 1."
          echo "Future publishing must use explicit validation and approved registry credentials."

