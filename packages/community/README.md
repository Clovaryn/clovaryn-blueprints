# Community Packages

Community blueprint packages are planned for a later phase.

Future community packages must be submitted by pull request, must be declarative, must include examples and tests, and must pass validation before publishing to any Clovaryn registry.

Do not submit secrets, credentials, generated Terraform output, Terraform state, shell hooks, Terraform provisioners, or private user data.

